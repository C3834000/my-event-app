# ============================================================================
# הפעלת סביבת הבדיקה המבודדת של מאגר המסמכים — פקודה אחת, אפס הגדרות.
# ----------------------------------------------------------------------------
# מריץ שרת בדיקה מקומי (פורט 4000) שמפעיל את פונקציית המסמכים האמיתית מול
# בסיס נתונים ואחסון מקומיים על הדיסק (תיקיית test-env), ומשיק את האפליקציה
# (פורט 3000). מפתח הגישה נוצר אקראית ונשמר ב-.env.documents-test (מחוץ ל-git).
# אין שום חיבור לייצור, לחשבונית ירוקה או למיילים.
#
# פתיחה: http://localhost:3000/#/documents
# ============================================================================
$ErrorActionPreference = 'Stop'
Set-Location (Split-Path -Parent $PSScriptRoot)
node --import ./scripts/local-supabase-register.mjs scripts/docs-test-server.mjs
